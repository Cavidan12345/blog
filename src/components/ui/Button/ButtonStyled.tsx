import styled from 'styled-components';
import { CircularProgress as Spinner } from '@mui/material';

export const CircularProgress = styled(Spinner)`
  width: 20px !important;
  height: 20px !important;
`;
